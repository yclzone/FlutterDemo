
import 'package:flutter/material.dart';

class FlutterAPage extends StatelessWidget {
  const FlutterAPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('FlutterA'),
      ),
    );
  }
}
