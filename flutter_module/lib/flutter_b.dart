import 'package:flutter/material.dart';

class FlutterBPage extends StatelessWidget {
  const FlutterBPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('FlutterB'),
      ),
    );
  }
}